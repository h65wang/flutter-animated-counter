name: animated_flip_counter
description: An implicit animation widget that flips from one number to another, with support for customize styles, decimals and negative values.
version: 0.3.0
homepage: https://github.com/h65wang/flutter-animated-counter

environment:
  sdk: ">=2.12.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

screenshots:
  - description: 'Animated flip counter (decimal)'
    path: 'gifs/percent.gif'
  - description: 'Animated flip counter (integer)'
    path: 'gifs/counter.gif'
  - description: 'Demo flip counter in an app'
    path: 'gifs/demo.gif'

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: